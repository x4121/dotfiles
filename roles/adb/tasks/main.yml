- name: Install
  become: true
  ansible.builtin.package:
    name: "{{ role_name }}"
    state: present

- name: Add to plugdev group
  become: true
  ansible.builtin.user:
    name: "{{ user }}"
    groups: "plugdev"
    append: true
